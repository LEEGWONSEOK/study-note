# 31. 성능 최적화

## 학습 목표
- React 성능 최적화 기법 이해하기
- 불필요한 리렌더링 방지 방법 학습하기
- 코드 분할과 지연 로딩 마스터하기
- 프로파일링과 디버깅 도구 활용하기

## React 렌더링 이해

### 비유로 이해하기
React 렌더링은 **건물 점검**과 같습니다:
- Virtual DOM: 건물 설계도
- Real DOM: 실제 건물
- Reconciliation: 설계도와 실제를 비교
- 변경사항만 수정: 전체를 다시 짓지 않고 필요한 부분만 수리

### 렌더링이 발생하는 경우

```javascript
function Component() {
    const [count, setCount] = useState(0);

    // 렌더링이 발생하는 경우:
    // 1. State 변경
    // 2. Props 변경
    // 3. 부모 컴포넌트 리렌더링
    // 4. Context 값 변경
    // 5. forceUpdate() 호출

    return <div>{count}</div>;
}
```

## React.memo: 컴포넌트 메모이제이션

`React.memo`는 **props가 변경되지 않으면 리렌더링을 건너뜁니다**.

### 기본 사용법

```javascript
// ❌ 최적화 전: 부모가 리렌더링되면 자식도 리렌더링
function Child({ name }) {
    console.log('Child 렌더링');
    return <div>{name}</div>;
}

function Parent() {
    const [count, setCount] = useState(0);
    const [name] = useState('김철수');

    return (
        <div>
            <button onClick={() => setCount(count + 1)}>
                Count: {count}
            </button>
            {/* count 변경 시 Child도 리렌더링됨 (name은 안 바뀌는데!) */}
            <Child name={name} />
        </div>
    );
}

// ✅ 최적화: React.memo 사용
const Child = React.memo(function Child({ name }) {
    console.log('Child 렌더링');
    return <div>{name}</div>;
});

// 이제 name이 변경될 때만 Child가 리렌더링됨
```

### 커스텀 비교 함수

```javascript
const UserCard = React.memo(
    function UserCard({ user }) {
        return (
            <div>
                <h3>{user.name}</h3>
                <p>{user.email}</p>
            </div>
        );
    },
    (prevProps, nextProps) => {
        // true 반환: 리렌더링 건너뜀
        // false 반환: 리렌더링 진행

        // user.id가 같으면 리렌더링 안 함
        return prevProps.user.id === nextProps.user.id;
    }
);
```

### 언제 사용할까?

```javascript
// ✅ React.memo 사용하기 좋은 경우
// 1. 자주 렌더링되는 큰 리스트의 항목
const ListItem = React.memo(({ item }) => {
    return <div>{item.name}</div>;
});

// 2. 복잡한 계산을 하는 컴포넌트
const ExpensiveComponent = React.memo(({ data }) => {
    const result = doExpensiveCalculation(data);
    return <div>{result}</div>;
});

// 3. Props가 자주 바뀌지 않는 컴포넌트
const Header = React.memo(() => {
    return <header>고정 헤더</header>;
});

// ❌ React.memo 불필요한 경우
// 1. 항상 다른 props를 받는 컴포넌트
function AlwaysChanging({ timestamp }) {
    return <div>{timestamp}</div>; // 매번 다른 값
}

// 2. 매우 간단한 컴포넌트
function Simple({ text }) {
    return <span>{text}</span>; // 메모이제이션 비용이 더 큼
}
```

## useMemo: 값 메모이제이션

```javascript
function ProductList({ products, category }) {
    // ❌ 매 렌더링마다 필터링 실행
    const filtered = products.filter(p => p.category === category);

    // ✅ category가 변경될 때만 필터링
    const filteredProducts = useMemo(() => {
        console.log('필터링 실행');
        return products.filter(p => p.category === category);
    }, [products, category]);

    return (
        <ul>
            {filteredProducts.map(product => (
                <li key={product.id}>{product.name}</li>
            ))}
        </ul>
    );
}

// 복잡한 계산 예시
function Statistics({ numbers }) {
    const stats = useMemo(() => {
        console.log('통계 계산 중...');

        const sum = numbers.reduce((a, b) => a + b, 0);
        const avg = sum / numbers.length;
        const sorted = [...numbers].sort((a, b) => a - b);
        const median = sorted[Math.floor(sorted.length / 2)];

        return { sum, avg, median };
    }, [numbers]);

    return (
        <div>
            <p>합계: {stats.sum}</p>
            <p>평균: {stats.avg}</p>
            <p>중앙값: {stats.median}</p>
        </div>
    );
}
```

## useCallback: 함수 메모이제이션

```javascript
function TodoList() {
    const [todos, setTodos] = useState([]);

    // ❌ 매 렌더링마다 새 함수 생성
    const handleDelete = (id) => {
        setTodos(todos.filter(todo => todo.id !== id));
    };

    // ✅ 함수 메모이제이션
    const handleDeleteOptimized = useCallback((id) => {
        setTodos(prev => prev.filter(todo => todo.id !== id));
    }, []); // 의존성 없음

    return (
        <ul>
            {todos.map(todo => (
                // React.memo와 함께 사용하면 효과적
                <TodoItem
                    key={todo.id}
                    todo={todo}
                    onDelete={handleDeleteOptimized}
                />
            ))}
        </ul>
    );
}

const TodoItem = React.memo(({ todo, onDelete }) => {
    console.log('TodoItem 렌더링:', todo.id);

    return (
        <li>
            {todo.text}
            <button onClick={() => onDelete(todo.id)}>삭제</button>
        </li>
    );
});
```

## 코드 분할 (Code Splitting)

### React.lazy와 Suspense

```javascript
import { lazy, Suspense } from 'react';

// ❌ 전통적인 import: 초기 번들에 포함
import HeavyComponent from './HeavyComponent';

// ✅ lazy import: 필요할 때만 로드
const HeavyComponent = lazy(() => import('./HeavyComponent'));

function App() {
    const [show, setShow] = useState(false);

    return (
        <div>
            <button onClick={() => setShow(true)}>
                컴포넌트 로드
            </button>

            {show && (
                <Suspense fallback={<div>로딩 중...</div>}>
                    <HeavyComponent />
                </Suspense>
            )}
        </div>
    );
}

// 라우트 기반 코드 분할
const Home = lazy(() => import('./pages/Home'));
const About = lazy(() => import('./pages/About'));
const Contact = lazy(() => import('./pages/Contact'));

function App() {
    return (
        <Router>
            <Suspense fallback={<div>페이지 로딩 중...</div>}>
                <Routes>
                    <Route path="/" element={<Home />} />
                    <Route path="/about" element={<About />} />
                    <Route path="/contact" element={<Contact />} />
                </Routes>
            </Suspense>
        </Router>
    );
}
```

### 에러 처리

```javascript
import { lazy, Suspense } from 'react';
import { ErrorBoundary } from 'react-error-boundary';

const LazyComponent = lazy(() => import('./LazyComponent'));

function App() {
    return (
        <ErrorBoundary
            fallback={<div>컴포넌트 로드 실패</div>}
            onError={(error) => console.error(error)}
        >
            <Suspense fallback={<div>로딩 중...</div>}>
                <LazyComponent />
            </Suspense>
        </ErrorBoundary>
    );
}
```

## 가상화 (Virtualization)

큰 리스트의 경우 **보이는 항목만 렌더링**합니다.

```javascript
// react-window 사용
import { FixedSizeList } from 'react-window';

function LargeList() {
    const items = Array.from({ length: 10000 }, (_, i) => `항목 ${i + 1}`);

    const Row = ({ index, style }) => (
        <div style={style}>
            {items[index]}
        </div>
    );

    return (
        <FixedSizeList
            height={600}          // 컨테이너 높이
            itemCount={items.length}  // 전체 항목 수
            itemSize={50}         // 각 항목 높이
            width="100%"
        >
            {Row}
        </FixedSizeList>
    );
}

// 동적 높이 리스트
import { VariableSizeList } from 'react-window';

function DynamicList() {
    const items = [
        { id: 1, text: '짧은 텍스트' },
        { id: 2, text: '긴 텍스트'.repeat(20) },
        { id: 3, text: '중간 텍스트'.repeat(5) }
    ];

    const getItemSize = (index) => {
        return items[index].text.length > 50 ? 100 : 50;
    };

    const Row = ({ index, style }) => (
        <div style={style}>
            {items[index].text}
        </div>
    );

    return (
        <VariableSizeList
            height={600}
            itemCount={items.length}
            itemSize={getItemSize}
            width="100%"
        >
            {Row}
        </VariableSizeList>
    );
}
```

## 실전 최적화 패턴

### 1. 디바운싱과 쓰로틀링

```javascript
// 디바운싱: 마지막 호출 후 일정 시간 대기
function SearchBox() {
    const [query, setQuery] = useState('');
    const [results, setResults] = useState([]);

    const debouncedSearch = useMemo(
        () =>
            debounce((searchQuery) => {
                fetch(`/api/search?q=${searchQuery}`)
                    .then(res => res.json())
                    .then(setResults);
            }, 500),
        []
    );

    const handleChange = (e) => {
        const value = e.target.value;
        setQuery(value);
        debouncedSearch(value);
    };

    return (
        <div>
            <input value={query} onChange={handleChange} />
            {/* 결과 표시 */}
        </div>
    );
}

function debounce(func, wait) {
    let timeout;
    return function (...args) {
        clearTimeout(timeout);
        timeout = setTimeout(() => func(...args), wait);
    };
}

// 쓰로틀링: 일정 시간 간격으로만 실행
function ScrollComponent() {
    const [scrollY, setScrollY] = useState(0);

    useEffect(() => {
        let ticking = false;

        const handleScroll = () => {
            if (!ticking) {
                window.requestAnimationFrame(() => {
                    setScrollY(window.scrollY);
                    ticking = false;
                });
                ticking = true;
            }
        };

        window.addEventListener('scroll', handleScroll);
        return () => window.removeEventListener('scroll', handleScroll);
    }, []);

    return <div>스크롤 위치: {scrollY}px</div>;
}
```

### 2. 이미지 지연 로딩

```javascript
function LazyImage({ src, alt }) {
    const [imageSrc, setImageSrc] = useState(null);
    const [isInView, setIsInView] = useState(false);
    const imgRef = useRef(null);

    useEffect(() => {
        const observer = new IntersectionObserver(
            ([entry]) => {
                if (entry.isIntersecting) {
                    setIsInView(true);
                    observer.disconnect();
                }
            },
            { threshold: 0.1 }
        );

        if (imgRef.current) {
            observer.observe(imgRef.current);
        }

        return () => {
            if (imgRef.current) {
                observer.unobserve(imgRef.current);
            }
        };
    }, []);

    useEffect(() => {
        if (isInView) {
            const img = new Image();
            img.src = src;
            img.onload = () => setImageSrc(src);
        }
    }, [isInView, src]);

    return (
        <div ref={imgRef} style={{ minHeight: '200px' }}>
            {imageSrc ? (
                <img src={imageSrc} alt={alt} />
            ) : (
                <div>로딩 중...</div>
            )}
        </div>
    );
}

// 네이티브 lazy loading
function NativeLazyImage({ src, alt }) {
    return <img src={src} alt={alt} loading="lazy" />;
}
```

### 3. 무한 스크롤 최적화

```javascript
function InfiniteScroll() {
    const [items, setItems] = useState([]);
    const [page, setPage] = useState(1);
    const [loading, setLoading] = useState(false);
    const [hasMore, setHasMore] = useState(true);

    const loadMoreRef = useRef(null);

    const loadMore = useCallback(async () => {
        if (loading || !hasMore) return;

        setLoading(true);

        try {
            const response = await fetch(`/api/items?page=${page}`);
            const data = await response.json();

            if (data.length === 0) {
                setHasMore(false);
            } else {
                setItems(prev => [...prev, ...data]);
                setPage(prev => prev + 1);
            }
        } finally {
            setLoading(false);
        }
    }, [page, loading, hasMore]);

    useEffect(() => {
        const observer = new IntersectionObserver(
            ([entry]) => {
                if (entry.isIntersecting) {
                    loadMore();
                }
            },
            { threshold: 0.5 }
        );

        if (loadMoreRef.current) {
            observer.observe(loadMoreRef.current);
        }

        return () => {
            if (loadMoreRef.current) {
                observer.unobserve(loadMoreRef.current);
            }
        };
    }, [loadMore]);

    return (
        <div>
            {items.map(item => (
                <ItemCard key={item.id} item={item} />
            ))}

            {loading && <div>로딩 중...</div>}

            {hasMore && <div ref={loadMoreRef} style={{ height: '20px' }} />}

            {!hasMore && <div>더 이상 항목이 없습니다</div>}
        </div>
    );
}
```

## 프로파일링과 디버깅

### React DevTools Profiler

```javascript
// Profiler로 성능 측정
import { Profiler } from 'react';

function onRenderCallback(
    id,           // 프로파일러 ID
    phase,        // "mount" 또는 "update"
    actualDuration,  // 렌더링 시간
    baseDuration,    // 메모이제이션 없이 걸리는 시간
    startTime,
    commitTime,
    interactions    // Set of interactions
) {
    console.log(`${id} - ${phase}: ${actualDuration}ms`);
}

function App() {
    return (
        <Profiler id="App" onRender={onRenderCallback}>
            <Main />
        </Profiler>
    );
}
```

### 렌더링 추적

```javascript
// 렌더링 횟수 추적
function useRenderCount() {
    const renderCount = useRef(0);

    useEffect(() => {
        renderCount.current += 1;
    });

    return renderCount.current;
}

function Component() {
    const renderCount = useRenderCount();
    console.log(`렌더링 횟수: ${renderCount}`);

    return <div>Component</div>;
}

// 렌더링 원인 추적
function useWhyDidYouUpdate(name, props) {
    const previousProps = useRef();

    useEffect(() => {
        if (previousProps.current) {
            const allKeys = Object.keys({ ...previousProps.current, ...props });
            const changedProps = {};

            allKeys.forEach(key => {
                if (previousProps.current[key] !== props[key]) {
                    changedProps[key] = {
                        from: previousProps.current[key],
                        to: props[key]
                    };
                }
            });

            if (Object.keys(changedProps).length > 0) {
                console.log('[why-did-you-update]', name, changedProps);
            }
        }

        previousProps.current = props;
    });
}

function Component(props) {
    useWhyDidYouUpdate('Component', props);
    return <div>Component</div>;
}
```

## 핵심 요약

### 1. 메모이제이션
```javascript
// 컴포넌트
const Comp = React.memo(MyComponent);

// 값
const value = useMemo(() => compute(), [deps]);

// 함수
const callback = useCallback(() => {}, [deps]);
```

### 2. 코드 분할
```javascript
// lazy import
const Comp = lazy(() => import('./Comp'));

// Suspense로 감싸기
<Suspense fallback={<Loading />}>
    <Comp />
</Suspense>
```

### 3. 최적화 체크리스트
- [ ] 불필요한 리렌더링 확인
- [ ] 큰 리스트는 가상화
- [ ] 이미지는 지연 로딩
- [ ] 무거운 컴포넌트는 코드 분할
- [ ] 검색/스크롤은 디바운싱/쓰로틀링

### 4. 성능 측정
- React DevTools Profiler
- Chrome DevTools Performance
- Lighthouse
- 렌더링 추적 Hook

## 연습 문제

### 문제 1: 검색 최적화
실시간 검색 기능을 최적화하세요.

```javascript
// 요구사항:
// - 500ms 디바운싱
// - 이전 요청 취소
// - 로딩 상태 표시
// - 결과 캐싱

function OptimizedSearch() {
    // 여기에 구현
}
```

### 문제 2: 큰 테이블 렌더링
10,000개 행을 가진 테이블을 최적화하세요.

```javascript
// 요구사항:
// - 가상 스크롤링
// - 정렬 기능
// - 필터링
// - 부드러운 스크롤

function LargeTable({ data }) {
    // 여기에 구현
}
```

### 문제 3: 이미지 갤러리
많은 이미지를 효율적으로 표시하세요.

```javascript
// 요구사항:
// - 지연 로딩
// - 썸네일 우선 로드
// - 무한 스크롤
// - 프리로딩

function ImageGallery({ images }) {
    // 여기에 구현
}
```

### 문제 4: 대시보드 최적화
여러 위젯이 있는 대시보드를 최적화하세요.

```javascript
// 요구사항:
// - 각 위젯 독립적 렌더링
// - 위젯별 데이터 캐싱
// - 실시간 업데이트 최적화
// - 코드 분할

function Dashboard() {
    // 여기에 구현
}
```

## 다음 단계
다음 장에서는 **고급 패턴**을 학습합니다:
- Render Props
- Higher-Order Components
- Compound Components
- Controlled vs Uncontrolled